{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug ourTool (lldb)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/ourTool",
      "args": [
        "${workspaceFolder}/tests/pseudo.c",
        "${workspaceFolder}/target.json"
      ],
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false,
      "environment": [],
      "externalConsole": false
    }
  ]
}